// Copy to config.js and include before app.js to point the frontend at your backend.
// Leave empty ("") to use same-origin (when frontend and backend are served from the same host).
window.YT_SAVER_API_BASE = "http://localhost:8000";
